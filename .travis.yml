language: php
php:
#  - 7.1
  - 7.0
#  - 5.6

sudo: false

cache:
  directories:
  - $HOME/.composer/cache

install:
  - composer --version
  - composer install -n
  - php ./tests/prepare_magento.sh

script:
  - ./run_tests.sh;
